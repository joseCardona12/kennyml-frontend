import "./verificationCodeTemplate.styles.scss";
import { FormVerificationCode } from "@/ui/organisms";

export default function VerificationCodeTemplate(): React.ReactNode {
  return (
    <div className="content-verification-code">
      <FormVerificationCode />
    </div>
  );
}
