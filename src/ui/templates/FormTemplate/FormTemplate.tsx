import { FormLogin } from "@/ui/organisms";
import "./formTemplate.styles.scss";

export default function FormTemplate(): React.ReactNode {
  return (
    <div className="content-form">
      <FormLogin />
    </div>
  );
}
